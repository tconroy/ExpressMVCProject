form#form-login(role='form' action='/login' method='POST')
  h1 Welcome.
  h2 Please sign in to continue.
  label.sr-only(for='username') Username
  input#username.form-control(type='text', name='username', placeholder='Username', required='true', autofocus='true')
  label.sr-only(for='password') Password
  input#password.form-control(type='password', name='password' placeholder='Password', required='true')
  .checkbox
  button#loginBtn.btn.btn-lg.btn-primary.btn-block(type='submit') Sign in
  p.small.pull-right
    | Not registered?
    a(href="/register") Register Now!